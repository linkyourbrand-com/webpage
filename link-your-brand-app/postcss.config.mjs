const config = {
  // Tailwind was present previously. If you're not using Tailwind, you can leave plugins empty.
  // To enable Tailwind again, uncomment the plugin below and ensure tailwindcss and @tailwindcss/postcss are installed.
  // plugins: ["@tailwindcss/postcss"],
  plugins: [],
};

export default config;
